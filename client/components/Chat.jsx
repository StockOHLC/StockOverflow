import React from "react";
//socket connection- might need to put in main component to establish connection on load
// const io = require('socket.io-client');
// const socket = io();

//make this into a stateful component?
const Chat = props => {
  return (
    <div className="chat-div">
      
    </div>
  );
};

export default Chat;
